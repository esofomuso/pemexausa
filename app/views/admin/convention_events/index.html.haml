%h1 Listing admin_convention_events

%table
  %thead
    %tr
      %th
      %th
      %th
      
  %tbody
    - @admin_convention_events.each do |admin_convention_event|
      %tr
        %td= link_to 'Show', admin_convention_event
        %td= link_to 'Edit', edit_admin_convention_event_path(admin_convention_event)
        %td= link_to 'Destroy', admin_convention_event, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Convention event', new_admin_convention_event_path
