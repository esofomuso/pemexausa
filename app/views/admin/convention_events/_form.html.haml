= form_for @admin_convention_event do |f|
  - if @admin_convention_event.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_convention_event.errors.count, "error")} prohibited this admin_convention_event from being saved:"
      %ul
        - @admin_convention_event.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
