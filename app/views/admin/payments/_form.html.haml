= form_for @admin_payment do |f|
  - if @admin_payment.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_payment.errors.count, "error")} prohibited this admin_payment from being saved:"
      %ul
        - @admin_payment.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
