= form_for @admin_state do |f|
  - if @admin_state.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_state.errors.count, "error")} prohibited this admin_state from being saved:"
      %ul
        - @admin_state.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
