= form_for @admin_member do |f|
  - if @admin_member.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_member.errors.count, "error")} prohibited this admin_member from being saved:"
      %ul
        - @admin_member.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
